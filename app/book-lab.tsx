import { BookLabScreen } from '@/features/book-lab';

export default function BookLabRoute() {
  return <BookLabScreen />;
}

export { RouteErrorBoundary as ErrorBoundary } from '@/components/route-error-boundary';
